[package]
name = "hashtable"
version = "0.0.0"
edition = "2021"
publish = false

[profile.dev.package."*"]
opt-level = 3
overflow-checks = true

[profile.test]
opt-level = 3
overflow-checks = false

[dependencies]
bumpalo = "3.10.0"
static_assertions = "1.1.0"

[dev-dependencies]
rand = { version = "0.8.5" }
